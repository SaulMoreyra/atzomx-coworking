# Google Places API (New) — reviews on the home page
# Enable "Places API (New)" in Google Cloud Console
GOOGLE_PLACES_API_KEY=

# Atzomx Café y Coworking — Oaxaca Centro
GOOGLE_PLACE_ID=ChIJa95gr88jx4URv2U6Ws0KZtQ

# PostgreSQL (local: docker compose up -d)
DATABASE_URL="postgresql://atzomx:atzomx@localhost:5432/atzomx?schema=public"

# JWT — generate with: openssl rand -base64 48
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Seed admin user (npm run db:seed)
ADMIN_SEED_EMAIL=admin@atzomx.com.mx
ADMIN_SEED_PASSWORD=ChangeMe123!
